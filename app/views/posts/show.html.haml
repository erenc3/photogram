= render 'post', post: @post
.text-center.edit-links
  - if @post.user.id == current_user.id
    = link_to "Geri", posts_path
    |
    = link_to "Post'u düzenle", edit_post_path(@post)
  - else
    =link_to "Geri", root_path
